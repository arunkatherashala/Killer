KPM (Killer Package Manager) - local dev

Usage:

```
cd tools/kpm
cargo run -- install path/to/manifest.json --dest ./installed
```

This crate contains a minimal installer plus remote download with SHA256 verification.
