// Test fibonacci(30) 
// This tests the language performance on recursive function calls

kfn fib(n) {
    if (n <= 1) {
        return n;
    } else {
        return fib(n - 1) + fib(n - 2);
    }
}

result = fib(30);
print("fib(30) =", result);


