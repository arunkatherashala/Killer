// Example: Fibonacci stress input for Killer Super compilation testing

kfn fib(n: Int) -> Int {
    if n <= 1 {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

kfn main() {
    let result: Int = fib(10000);
    println("fib(10000) = " + result.to_string());
}

